# 类型定义

<cite>
**本文档引用的文件**
- [src/core/types.ts](file://src/core/types.ts)
- [src/core/config.ts](file://src/core/config.ts)
- [src/core/errors.ts](file://src/core/errors.ts)
- [src/generator/styles.ts](file://src/generator/styles.ts)
- [src/generator/renderers/block.ts](file://src/generator/renderers/block.ts)
- [src/generator/renderers/inline.ts](file://src/generator/renderers/inline.ts)
- [src/generator/document-builder.ts](file://src/generator/document-builder.ts)
- [src/parser/tokenize.ts](file://src/parser/tokenize.ts)
- [src/parser/transformer.ts](file://src/parser/transformer.ts)
- [src/utils/units.ts](file://src/utils/units.ts)
- [src/utils/image.ts](file://src/utils/image.ts)
- [src/wopi/storage.ts](file://src/wopi/storage.ts)
- [src/index.ts](file://src/index.ts)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心类型系统](#核心类型系统)
4. [文档中间表示类型](#文档中间表示类型)
5. [配置类型系统](#配置类型系统)
6. [渲染器类型定义](#渲染器类型定义)
7. [工具函数类型](#工具函数类型)
8. [错误类型系统](#错误类型系统)
9. [WOPi存储类型](#wopi存储类型)
10. [导出接口](#导出接口)
11. [类型关系图](#类型关系图)

## 简介

本文件详细分析了 Markdown 转 Word 文档转换系统的类型定义体系。该系统采用 TypeScript 构建，具有完整的类型安全性和严格的接口约束。本文档将深入解析核心类型、配置系统、渲染器架构以及相关的工具函数类型定义。

## 项目结构

该代码库采用模块化架构，主要包含以下核心模块：

```mermaid
graph TB
subgraph "核心模块"
Types[类型定义<br/>src/core/types.ts]
Config[配置系统<br/>src/core/config.ts]
Errors[错误处理<br/>src/core/errors.ts]
end
subgraph "解析模块"
Tokenize[标记化<br/>src/parser/tokenize.ts]
Transformer[转换器<br/>src/parser/transformer.ts]
end
subgraph "生成模块"
Styles[样式系统<br/>src/generator/styles.ts]
BlockRenderer[块级渲染器<br/>src/generator/renderers/block.ts]
InlineRenderer[内联渲染器<br/>src/generator/renderers/inline.ts]
Builder[文档构建器<br/>src/generator/document-builder.ts]
end
subgraph "工具模块"
Units[单位转换<br/>src/utils/units.ts]
ImageUtils[图像处理<br/>src/utils/image.ts]
WopiStorage[WOPi存储<br/>src/wopi/storage.ts]
end
Types --> Tokenize
Tokenize --> Transformer
Transformer --> BlockRenderer
BlockRenderer --> Builder
InlineRenderer --> BlockRenderer
Styles --> Builder
Units --> Styles
Units --> BlockRenderer
Units --> InlineRenderer
ImageUtils --> BlockRenderer
WopiStorage --> Builder
```

**图表来源**
- [src/core/types.ts:1-204](file://src/core/types.ts#L1-L204)
- [src/core/config.ts:1-91](file://src/core/config.ts#L1-L91)
- [src/parser/tokenize.ts:1-16](file://src/parser/tokenize.ts#L1-L16)
- [src/parser/transformer.ts:1-359](file://src/parser/transformer.ts#L1-L359)

## 核心类型系统

### 文档元数据类型

系统使用 `DocumentMeta` 接口定义文档的基本元信息：

```mermaid
classDiagram
class DocumentMeta {
+string title?
+string author?
+string date?
}
class DocumentIR {
+string type
+DocumentMeta meta
+ResolvedConfig config
+BlockNode[] children
}
DocumentIR --> DocumentMeta : "包含"
```

**图表来源**
- [src/core/types.ts:1-12](file://src/core/types.ts#L1-L12)

### 块级节点类型

系统定义了完整的块级节点类型层次结构，支持 Markdown 的所有块级元素：

```mermaid
classDiagram
class BlockNode {
<<union type>>
}
class HeadingNode {
+string type
+1|2|3|4|5|6 level
+InlineNode[] children
}
class ParagraphNode {
+string type
+InlineNode[] children
}
class ListNode {
+string type
+boolean ordered
+number start?
+ListItemNode[] children
}
class BlockquoteNode {
+string type
+BlockNode[] children
}
class CodeBlockNode {
+string type
+string language?
+string value
}
class TableNode {
+string type
+TableRowNode[] children
}
class ImageNode {
+string type
+string src
+string alt?
+number width?
+number height?
+left|center|right align?
}
class ThematicBreakNode {
+string type
}
BlockNode <|-- HeadingNode
BlockNode <|-- ParagraphNode
BlockNode <|-- ListNode
BlockNode <|-- BlockquoteNode
BlockNode <|-- CodeBlockNode
BlockNode <|-- TableNode
BlockNode <|-- ImageNode
BlockNode <|-- ThematicBreakNode
```

**图表来源**
- [src/core/types.ts:14-89](file://src/core/types.ts#L14-L89)

### 内联节点类型

内联节点类型定义了文本格式化的基础元素：

```mermaid
classDiagram
class InlineNode {
<<union type>>
}
class TextNode {
+string type
+string value
}
class BoldNode {
+string type
+InlineNode[] children
}
class ItalicNode {
+string type
+InlineNode[] children
}
class UnderlineNode {
+string type
+InlineNode[] children
}
class InlineCodeNode {
+string type
+string value
}
class LinkNode {
+string type
+string href
+InlineNode[] children
}
class LineBreakNode {
+string type
}
class StrikethroughNode {
+string type
+InlineNode[] children
}
InlineNode <|-- TextNode
InlineNode <|-- BoldNode
InlineNode <|-- ItalicNode
InlineNode <|-- UnderlineNode
InlineNode <|-- InlineCodeNode
InlineNode <|-- LinkNode
InlineNode <|-- LineBreakNode
InlineNode <|-- StrikethroughNode
```

**图表来源**
- [src/core/types.ts:91-140](file://src/core/types.ts#L91-L140)

**章节来源**
- [src/core/types.ts:1-204](file://src/core/types.ts#L1-L204)

## 文档中间表示类型

### 解析流程类型

系统通过中间表示（IR）在解析和生成之间传递数据：

```mermaid
sequenceDiagram
participant Parser as Markdown解析器
participant IR as 文档IR
participant Renderer as 渲染器
participant DOCX as DOCX文档
Parser->>Parser : tokenize(markdown)
Parser->>Parser : transformTokens(tokens)
Parser->>IR : 创建DocumentIR对象
IR->>Renderer : renderBlock(node, config)
Renderer->>DOCX : 生成段落/表格等元素
Renderer->>DOCX : 应用样式和格式
DOCX->>DOCX : 组装完整文档
```

**图表来源**
- [src/parser/tokenize.ts:12-15](file://src/parser/tokenize.ts#L12-L15)
- [src/parser/transformer.ts:25-39](file://src/parser/transformer.ts#L25-L39)
- [src/generator/document-builder.ts:18-29](file://src/generator/document-builder.ts#L18-L29)

**章节来源**
- [src/parser/tokenize.ts:1-16](file://src/parser/tokenize.ts#L1-L16)
- [src/parser/transformer.ts:1-359](file://src/parser/transformer.ts#L1-L359)
- [src/generator/document-builder.ts:1-193](file://src/generator/document-builder.ts#L1-L193)

## 配置类型系统

### 配置架构

系统使用 Zod 进行配置验证，确保运行时配置的完整性：

```mermaid
classDiagram
class ResolvedConfig {
+FontConfig font
+SizeConfig size
+SpacingConfig spacing
+MarginConfig margin
+ImageConfig image
+HeaderFooterConfig headerFooter
+ColorConfig color
+A4|Letter pageSize
+portrait|landscape orientation
}
class FontConfig {
+string body
+string heading
+string english
+string code
}
class SizeConfig {
+number body
+number heading1
+number heading2
+number heading3
+number heading4
+number heading5
+number heading6
+number code
}
class SpacingConfig {
+number lineSpacing
+number paragraphSpacing
+number headingSpacing
}
class MarginConfig {
+number top
+number bottom
+number left
+number right
}
class ImageConfig {
+number maxWidthPercent
+left|center|right defaultAlign
}
class HeaderFooterConfig {
+string header?
+string footer?
+boolean pageNumbers?
}
class ColorConfig {
+string heading
+string text
+string link
+string codeBackground
+string blockquoteBorder
}
ResolvedConfig --> FontConfig
ResolvedConfig --> SizeConfig
ResolvedConfig --> SpacingConfig
ResolvedConfig --> MarginConfig
ResolvedConfig --> ImageConfig
ResolvedConfig --> HeaderFooterConfig
ResolvedConfig --> ColorConfig
```

**图表来源**
- [src/core/types.ts:143-203](file://src/core/types.ts#L143-L203)
- [src/core/config.ts:4-64](file://src/core/config.ts#L4-L64)

### 配置验证流程

```mermaid
flowchart TD
Start([配置输入]) --> Schema[Zod配置模式]
Schema --> Parse[解析配置]
Parse --> Validate{验证通过?}
Validate --> |是| Merge[合并默认值]
Validate --> |否| Error[抛出配置验证错误]
Merge --> Create[创建ResolvedConfig]
Create --> End([配置就绪])
Error --> End
```

**图表来源**
- [src/core/config.ts:68-81](file://src/core/config.ts#L68-L81)
- [src/core/errors.ts:22-27](file://src/core/errors.ts#L22-L27)

**章节来源**
- [src/core/config.ts:1-91](file://src/core/config.ts#L1-L91)
- [src/core/types.ts:142-203](file://src/core/types.ts#L142-L203)

## 渲染器类型定义

### 块级渲染器类型

块级渲染器负责将块级节点转换为 DOCX 元素：

```mermaid
classDiagram
class BlockNode {
<<union type>>
}
class Paragraph {
+TextRun[] children
+ISpacingProperties spacing
+AlignmentType alignment?
}
class Table {
+TableRow[] rows
+ITableBorders borders
+ITableWidth width
}
class TableRow {
+TableCell[] children
}
class TableCell {
+Paragraph[] children
+ICellShading shading?
}
class HeadingLevel {
<<枚举>>
HEADING_1
HEADING_2
HEADING_3
HEADING_4
HEADING_5
HEADING_6
}
BlockNode --> Paragraph
BlockNode --> Table
BlockNode --> TableRow
BlockNode --> TableCell
```

**图表来源**
- [src/generator/renderers/block.ts:13-26](file://src/generator/renderers/block.ts#L13-L26)

### 内联渲染器类型

内联渲染器处理文本格式化：

```mermaid
classDiagram
class InlineNode {
<<union type>>
}
class TextRun {
+string text
+IFontOptions font?
+number size?
+boolean bold?
+boolean italics?
+IUnderlineOptions underline?
+boolean strike?
+string color?
+ICellShading shading?
+number break?
}
class ExternalHyperlink {
+TextRun[] children
+string link
}
class TextStyle {
+IFontOptions font?
+number size?
+boolean bold?
+boolean italics?
+IUnderlineOptions underline?
+boolean strike?
+string color?
+ICellShading shading?
}
InlineNode --> TextRun
InlineNode --> ExternalHyperlink
TextStyle --> TextRun
```

**图表来源**
- [src/generator/renderers/inline.ts:5-12](file://src/generator/renderers/inline.ts#L5-L12)

**章节来源**
- [src/generator/renderers/block.ts:1-286](file://src/generator/renderers/block.ts#L1-L286)
- [src/generator/renderers/inline.ts:1-128](file://src/generator/renderers/inline.ts#L1-L128)

## 工具函数类型

### 单位转换类型

系统提供精确的单位转换函数：

```mermaid
classDiagram
class UnitConverter {
+pxToEmu(px : number) number
+ptToHalfPt(pt : number) number
+ptToTwip(pt : number) number
+getPageWidthEmu(size : A4|Letter, orientation : portrait|landscape) number
+getPageHeightEmu(size : A4|Letter, orientation : portrait|landscape) number
}
class PageSize {
<<枚举>>
A4
Letter
}
class Orientation {
<<枚举>>
portrait
landscape
}
UnitConverter --> PageSize
UnitConverter --> Orientation
```

**图表来源**
- [src/utils/units.ts:6-44](file://src/utils/units.ts#L6-L44)

### 图像处理类型

图像处理功能包含完整的元数据类型：

```mermaid
classDiagram
class ImageMetadata {
+number width
+number height
+Buffer buffer
+string extension
}
class ImageProcessor {
+readImage(src : string) Promise~ImageMetadata~
+calculateScaledDimensions(width : number, height : number, maxWidth : number) Dimensions
}
ImageProcessor --> ImageMetadata
```

**图表来源**
- [src/utils/image.ts:5-10](file://src/utils/image.ts#L5-L10)

**章节来源**
- [src/utils/units.ts:1-45](file://src/utils/units.ts#L1-L45)
- [src/utils/image.ts:1-58](file://src/utils/image.ts#L1-L58)

## 错误类型系统

系统定义了完整的错误类型层次结构：

```mermaid
classDiagram
class Error {
+string name
+string message
}
class MarkdownParseError {
+string source?
}
class DocxGenerationError {
+unknown cause?
}
class ImageProcessingError {
+string src
+unknown cause?
}
class ConfigValidationError {
+unknown[] issues?
}
Error <|-- MarkdownParseError
Error <|-- DocxGenerationError
Error <|-- ImageProcessingError
Error <|-- ConfigValidationError
```

**图表来源**
- [src/core/errors.ts:1-27](file://src/core/errors.ts#L1-L27)

**章节来源**
- [src/core/errors.ts:1-28](file://src/core/errors.ts#L1-L28)

## WOPi存储类型

WOPi 存储系统提供了临时文件管理功能：

```mermaid
classDiagram
class WopiStorage {
+save(fileId : string, buffer : Buffer) Promise~void~
+read(fileId : string) Promise~Buffer~
+remove(fileId : string) Promise~void~
+getMeta(fileId : string) FileMeta?
+updateModified(fileId : string) Promise~void~
+getLock(fileId : string) string?
+setLock(fileId : string, lockId : string, ttlMs? : number) void
+deleteLock(fileId : string) void
}
class FileMeta {
+string path
+number createdAt
+number size
+number modifiedAt
}
class LockInfo {
+string lockId
+number expiresAt
}
WopiStorage --> FileMeta
WopiStorage --> LockInfo
```

**图表来源**
- [src/wopi/storage.ts:9-10](file://src/wopi/storage.ts#L9-L10)

**章节来源**
- [src/wopi/storage.ts:1-81](file://src/wopi/storage.ts#L1-L81)

## 导出接口

主入口文件统一导出了所有公共类型：

```mermaid
graph LR
Export[主入口导出] --> Types[类型定义]
Export --> Config[配置类型]
Export --> Errors[错误类型]
Export --> Utils[工具类型]
Types --> DocumentIR
Types --> BlockNode
Types --> InlineNode
Types --> ResolvedConfig
Config --> ConfigInput
Config --> FontConfig
Config --> SizeConfig
Errors --> MarkdownParseError
Errors --> DocxGenerationError
Errors --> ImageProcessingError
Errors --> ConfigValidationError
Utils --> ImageMetadata
Utils --> UnitConverters
```

**图表来源**
- [src/index.ts:5-24](file://src/index.ts#L5-L24)

**章节来源**
- [src/index.ts:1-25](file://src/index.ts#L1-L25)

## 类型关系图

系统整体类型关系如下：

```mermaid
erDiagram
DocumentIR {
string type
DocumentMeta meta
ResolvedConfig config
}
BlockNode {
string type
}
InlineNode {
string type
}
ResolvedConfig {
FontConfig font
SizeConfig size
SpacingConfig spacing
MarginConfig margin
ImageConfig image
HeaderFooterConfig headerFooter
ColorConfig color
string pageSize
string orientation
}
DocumentIR ||--o{ BlockNode : "包含"
BlockNode ||--o{ InlineNode : "包含"
ResolvedConfig ||--|| FontConfig : "包含"
ResolvedConfig ||--|| SizeConfig : "包含"
ResolvedConfig ||--|| SpacingConfig : "包含"
ResolvedConfig ||--|| MarginConfig : "包含"
ResolvedConfig ||--|| ImageConfig : "包含"
ResolvedConfig ||--|| HeaderFooterConfig : "包含"
ResolvedConfig ||--|| ColorConfig : "包含"
```

**图表来源**
- [src/core/types.ts:7-203](file://src/core/types.ts#L7-L203)

该类型定义系统展现了现代 TypeScript 应用的典型特征：严格的类型约束、清晰的接口分离、完善的错误处理机制，以及模块化的架构设计。通过这些类型定义，开发者可以确保代码的类型安全性，同时获得良好的开发体验和维护性。